# Browser

<div><pre>┌─────┐      ┌──────┐       ┌─────┐    css    ┌──────────┐        ┌──────────┐        ┌────────┐
│ URL │ HTTP │ HTML │ parse │ DOM │ computing │ DOM with │ layout │ DOM with │ render │ BitMap │
│     │─────>│      │──────>│     │──────────>│    css   │───────>│ position │───────>│        │
└─────┘      └──────┘       └─────┘           └──────────┘        └──────────┘        └────────┘</pre></div>

